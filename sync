#!/bin/bash
mkdir -p /tmp/rom # Where to sync source
cd /tmp/rom

# Initialize local repository
repo init -u https://github.com/PixelOS-Pixelish/manifest -b eleven

# Sync make sure j = 8
repo sync -c -j8 --force-sync --no-clone-bundle --no-tags

#Trees 
git clone https://github.com/shield44/device_xiaomi_olivewood -b ppui device/xiaomi/olivewood
git clone https://github.com/shield44/vendor_xiaomi_olivewood -b 11 vendor/xiaomi/olives
git clone https://github.com/shield44/kernel_xiaomi_olivewood -b eleven kernel/xiaomi/sdm439
